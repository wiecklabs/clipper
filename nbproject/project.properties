.ssh.dir=${file.reference.clipper-.ssh}
burn.dir=${file.reference.clipper-burn}
clipper.dir=${file.reference.clipper-clipper}
file.reference.clipper-.git=.git
file.reference.clipper-.ssh=.ssh
file.reference.clipper-burn=burn
file.reference.clipper-clipper=clipper
file.reference.clipper-lib=lib
file.reference.clipper-scratch=scratch
file.reference.clipper-script=script
file.reference.clipper-tests=tests
lib.dir=${file.reference.clipper-lib}
main.file=
platform.active=JRuby
scratch.dir=${file.reference.clipper-scratch}
script.dir=${file.reference.clipper-script}
source.encoding=UTF-8
src.dir=${file.reference.clipper-.git}
test.src.dir=${file.reference.clipper-tests}
